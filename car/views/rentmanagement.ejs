<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Vintage Car</title>

    <style>
        .category {
            font-weight:bolder;
            font-size: large;
        }
        .box {
            width: 300px;
            height: auto;
        }
        .greyed-out {
            opacity: 0.5; /* Set opacity to make image appear greyed out */
        }
        .navbar .navbar-nav .nav-link .icon {
            display: inline-block;
            width: 20px; /* Adjust the size as needed */
            height: 20px; /* Adjust the size as needed */
            margin-right: 10px;
        }
        .card {
            width: 300px;
            height: 350px; /* Increased height to accommodate the edit button */
            position: relative;
            overflow: hidden;
            margin-bottom: 20px;
            border-bottom: 15px solid orange; /* Set thicker orange border at the bottom */
        }
    
        .card img {
            width: 100%; /* Make the image fill the entire width of the card */
            height: 50%; /* Set height to 50% of the card height */
            object-fit: cover; /* Ensure the image covers the entire space */
            object-position: top center; /* Position the image at the top center */
        }
    
        .card-body {
            padding: 10px;
            height: calc(50% - 20px); /* Set height to 50% of the card height minus padding */
            box-sizing: border-box; /* Include padding and border in height calculation */
            overflow: auto; /* Add overflow for scrolling if needed */
        }
        .edit-icon {
            position: absolute;
            bottom: 5px; /* Adjust vertical position */
            right: 10px; /* Adjust horizontal position */
            width: 20px;
            height: 40px;
            z-index: 999; /* Ensure edit icon appears on top of other content */
        }

        #plus-button-container {
            position: fixed;
            bottom: 80px;
            right: 80px;
        }

        #yellow-button {
            background-color: orange;
            color: white;
            border: none;
            border-radius: 50%;
            width: 45px;
            height: 45px;
            font-size: 30px;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        
        .popup-container {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            height:200px;
            transform: translate(-50%, -50%);
            background-color: white;
            border: 1px solid #ccc;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px; /* Change padding for smaller pop-up */
            z-index: 9999;
            border-radius: 10px; /* Add rounded corners to pop-up box */
        }
        .popup-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            position: relative; /* Set position relative for close button positioning */
            height: 100%;
        }
        .close-btn {
            cursor: pointer;
            position: absolute;
            top: 5px;
            right: 5px;
        }
    </style>
</head>
<body>
<div class="container mt-4 mb-4">
    <!-- Navigation Bar -->
    <div class="row">
        <nav class="navbar navbar-expand-lg navbar-light bg-light w-100">
            <span class="navbar-brand mb-0 h1">VintageCar</span>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-item nav-link active" href="http://127.0.0.1:5002/">Home</a>
                    <a id="about-link" class="nav-item nav-link" href="#">About Us</a>
                    <a class="nav-item nav-link" href="http://127.0.0.1:5006/forum">Community Forum</a>
                    <a class="nav-item nav-link" href="http://127.0.0.1:5005/buyer_order">Orders</a>
                    <a class="nav-item nav-link" href="http://127.0.0.1:5001/rentcar">Car Rental</a>
                </div>
            </div>
            <div class="navbar-nav ml-auto">
                <a class="nav-item nav-link" href="#">
                    <img src="/Cart.png" alt="Cart" class="icon">
                    Cart
                </a>
                <a class="nav-item nav-link" href="#">
                    <img src="/message-icon.png" alt="Messages" class="icon">
                    Messages
                </a>
                <div class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown"
                       aria-haspopup="true" aria-expanded="false">
                        <img src="/user-profile.png" alt="Profile" class="icon">
                        Profile
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="#">Account Settings</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="http://127.0.0.1:5001/logout">Logout</a>
                    </div>
                </div>
            </div>
            <form class="form-inline">
                <button id="sellBtn" class="btn btn-success" type="button" onclick="toggleButton()">Sell</button>
            </form>
        </nav>
    </div>

    <div class="container">
        <div class="row">
            <div style="font-weight: bolder; font-size: x-large; text-align: center;">Manage Car Rental</div>
        </div>
    </div>
        
    <div class="container">
        <% if (carData && carData.Cars && carData.Cars.length > 0) { %>
        <span class="category">Car Rental Details</span>
        <table class="table table-bordered">
            <!-- Table headers -->
            <thead>
                <tr>
                    <th>Model</th>
                    <th>Brand</th>
                    <th>VIN</th>
                    <th>Description</th>
                    <th>Price/hour</th>
                    <th>Location</th>
                    <th>Availability</th>
                    <th>Manage</th>
                    <th>Edit</th>
                    <th>View</th>
                </tr>
            </thead>
            <tbody>
                    <% carData.Cars.forEach(function(car) { %>
                        <!-- Table data -->
                        <tr>
                            <td><%= car.Model %></td>
                            <td><%= car.Brand %></td>
                            <td><%= car.VehicleIdentificationNum %></td>
                            <td><%= car.Description %></td>
                            <td>$<%= car.Price %></td>
                            <td><%= car.Location %></td>
                            <td><%= car.Availability ? 'Available' : 'On Rent' %></td>
                            <td>
                                <!-- Manage button -->
                                <button type="button" class="btn btn-primary rent-btn" data-vin="<%= car.VehicleIdentificationNum %>"
                                    data-availability="<%= car.Availability %>">Rent</button>
                            </td>
                            <td>
                                
                                <!-- Hidden input fields for data -->
                                <form id="editForm_<%= car.VehicleIdentificationNum %>" action="/updatecar" method="POST" style="display: none;">
                                    <!-- Hidden input fields for data -->
                                    <input type="hidden" name="Model" value="<%= car.Model %>">
                                    <input type="hidden" name="Brand" value="<%= car.Brand %>">
                                    <input type="hidden" name="VehicleIdentificationNum" value="<%= car.VehicleIdentificationNum %>">
                                    <input type="hidden" name="Description" value="<%= car.Description %>">
                                    <input type="hidden" name="Price" value="<%= car.Price %>">
                                    <input type="hidden" name="Location" value="<%= car.Location %>">
                                </form>
                                <!-- Button to trigger form submission -->
                                <button type="button" class="btn btn-primary edit-btn" data-formid="editForm_<%= car.VehicleIdentificationNum %>" data-sellerid="<%= car.SellerID %>">Edit</button>
                            </td>
                            <td>
                                <button type="button" class="btn btn-primary view-btn" data-vin="<%= car.VehicleIdentificationNum %>">View</button>
                            </td>
                        </tr>
                    <% }); %>
            </tbody>
        </table>
        <% } else { %>
            <!-- Message if no cars found -->
            </br><span>There are no cars listed for rent.</span>
        <% } %>
    </div>
</div>

    <!-- Pop-up Box -->
    <div id="popup" class="popup-container">
        <div class="popup-content">
            <span id="popup-text">Welcome to XXX, where opulence meets automotive excellence.

                Introducing a bespoke platform tailored exclusively for discerning connoisseurs of vintage automobiles, addressing the distinct challenges faced by owners of luxury classics. Our singular platform offers three distinct experiences:
                </span>
            <span class="close-btn" onclick="closePopup()">X</span>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Add click event listener to edit buttons
            $('.edit-btn').click(function() {
                var formId = $(this).data('formid');
                var rowData = getRowData($(this));
                if (rowData) {
                    // Store the row data in session storage
                    sessionStorage.setItem('rowData', JSON.stringify(rowData));
                    // Submit the form to updatecar page
                    $('#' + formId).submit();
                } else {
                    console.error('rowData is null or empty.');
                }
            });

            // Function to get rowData associated with the clicked edit button
            function getRowData(button) {
                var rowData = {};
                var row = button.closest('tr');
                rowData.Model = row.find('td:nth-child(1)').text().trim();
                rowData.Brand = row.find('td:nth-child(2)').text().trim();
                rowData.VIN = row.find('td:nth-child(3)').text().trim();
                rowData.Description = row.find('td:nth-child(4)').text().trim();
                rowData.Price = row.find('td:nth-child(5)').text().trim();
                rowData.Location = row.find('td:nth-child(6)').text().trim();
                rowData.Availability = row.find('td:nth-child(7)').text().trim();
                rowData.SellerID = button.data('sellerid'); // Retrieve SellerID from the data attribute of the edit button
                return rowData;
            }

            // Add event listener to rent buttons
            $('.container').on('click', '.rent-btn', function() {
                var button = $(this); // Store the button element
                var vin = button.data('vin'); // Retrieve VIN from data-vin attribute

                // Make POST request to your server
                $.ajax({
                    url: 'http://127.0.0.1:5009/car/rent/' + vin,
                    type: 'POST', // Assuming the server expects a POST request
                    success: function(response) {
                        // Handle successful response
                        // Change button text to 'Return'
                        button.text('Return');
                        // Change button class to 'return-btn'
                        button.removeClass('rent-btn').addClass('return-btn');
                        // Update availability text to 'On Rent'
                        button.closest('tr').find('td:nth-child(7)').text('On Rent');
                        // Update data-availability attribute
                        button.data('availability', 'On Rent');
                    },
                    error: function(xhr, status, error) {
                        console.error(error);
                    }
                });
            });

            // Add event listener to return buttons
            $('.container').on('click', '.return-btn', function() {
                var button = $(this); // Store the button element
                var vin = button.data('vin'); // Retrieve VIN from data-vin attribute

                // Make POST request to your server to return the car
                $.ajax({
                    url: 'http://127.0.0.1:5009/car/return/' + vin,
                    type: 'POST', // Assuming the server expects a POST request to return the car
                    success: function(response) {
                        // Handle successful response
                        // Change button text to 'Rent'
                        button.text('Rent');
                        // Change button class to 'rent-btn'
                        button.removeClass('return-btn').addClass('rent-btn');
                        // Update availability text to 'Available'
                        button.closest('tr').find('td:nth-child(7)').text('Available');
                        // Update data-availability attribute
                        button.data('availability', 'Available');
                    },
                    error: function(xhr, status, error) {
                        console.error(error);
                    }
                });
            });

            // Add event listener to view buttons
            $('.container').on('click', '.view-btn', function() {
                var vin = $(this).data('vin'); // Retrieve VIN from data-vin attribute
                // Redirect to the car details page
                window.location.href = 'http://127.0.0.1:5009/car/' + vin;
            });
        });

        function toggleButton() {
            const button = document.getElementById('sellBtn');
            const plusButtonContainer = document.getElementById('plus-button-container');
            const sellForm = document.getElementById('sellForm');

            if (isSell) {
                button.classList.remove('btn-outline-danger');
                button.classList.add('btn-success');
                button.textContent = 'Buy';
                plusButtonContainer.style.display = 'block'; // Show the plus button container
                // Submit the form to redirect to /listing
                sellForm.submit();
            } else {
                button.classList.remove('btn-success');
                button.classList.add('btn-outline-danger');
                button.textContent = 'Sell';
                plusButtonContainer.style.display = 'none'; // Hide the plus button container
                // Redirect to another URL
                window.location.href = "/another-url";
            }
            isSell = !isSell;
        }

        // Add event listener to the Buy button
        document.querySelector('button.btn-success').addEventListener('click', function() {
            window.location.href = "http://127.0.0.1:5002/";
        });

        document.getElementById('about-link').addEventListener('click', function(event) {
            event.preventDefault();
            document.getElementById('popup').style.display = 'block';
        });

        function closePopup() {
            document.getElementById('popup').style.display = 'none';
        }
    </script>
</body>
</html>
